<Window x:Class="CopiMineLauncher.App.LaunchFailureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Minecraft не запустился"
        Width="700"
        Height="560"
        MinWidth="580"
        MinHeight="460"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Background="{StaticResource LauncherWindowBackground}"
        Foreground="{StaticResource LauncherPrimaryText}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ПРОБЛЕМА ЗАПУСКА" Foreground="{StaticResource LauncherAccent}" FontSize="11" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Text="{Binding Title}" FontFamily="{StaticResource LauncherDisplayFont}" FontSize="26" FontWeight="Bold" Margin="0,5,0,0" />
        <TextBlock Grid.Row="2" Text="{Binding Summary}" Foreground="{StaticResource LauncherSecondaryText}" FontSize="15" TextWrapping="Wrap" Margin="0,8,0,0" />

        <Border Grid.Row="3" Background="{StaticResource LauncherSurface}" BorderBrush="{StaticResource LauncherLine}" BorderThickness="0,1,0,1" Padding="14" Margin="0,16,0,14">
            <StackPanel>
                <TextBlock Text="Что произошло" Foreground="{StaticResource LauncherAccentPurple}" FontWeight="Bold" />
                <TextBlock Text="{Binding Explanation}" Foreground="{StaticResource LauncherPrimaryText}" TextWrapping="Wrap" Margin="0,6,0,0" />
                <TextBlock Text="{Binding ModHint}" Foreground="{StaticResource LauncherAccentAmber}" TextWrapping="Wrap" Margin="0,9,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Фрагменты лога" Foreground="{StaticResource LauncherMutedText}" FontWeight="Bold" />
            <TextBox Grid.Row="1"
                     Text="{Binding EvidenceText}"
                     IsReadOnly="True"
                     IsTabStop="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="{StaticResource LauncherInputSurface}"
                     Foreground="{StaticResource LauncherPrimaryText}"
                     BorderBrush="{StaticResource LauncherBorder}"
                     BorderThickness="1"
                     Padding="10"
                     FontFamily="{StaticResource LauncherDataFont}"
                     FontSize="12"
                     Margin="0,7,0,8" />
            <TextBlock Grid.Row="2" Text="{Binding LogPath}" Foreground="{StaticResource LauncherMutedText}" FontSize="11" TextTrimming="CharacterEllipsis" ToolTip="{Binding LogPath}" />
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Style="{StaticResource LauncherPrimaryButton}" Content="Открыть полный лог" Click="OpenLog_Click" />
            <Button Content="Закрыть" IsDefault="True" IsCancel="True" Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
