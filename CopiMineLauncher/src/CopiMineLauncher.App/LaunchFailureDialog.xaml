<Window x:Class="CopiMineLauncher.App.LaunchFailureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Minecraft не запустился"
        Width="700"
        Height="560"
        MinWidth="580"
        MinHeight="460"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        Background="#09111a"
        Foreground="#eef7f5">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#eef7f5" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#183344" />
            <Setter Property="Foreground" Value="#eef7f5" />
            <Setter Property="BorderBrush" Value="#2b5367" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,9" />
            <Setter Property="Margin" Value="0,0,10,0" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#217b59" />
            <Setter Property="BorderBrush" Value="#48d891" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ПРОБЛЕМА ЗАПУСКА" Foreground="#63dfa0" FontSize="11" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="26" FontWeight="Bold" Margin="0,5,0,0" />
        <TextBlock Grid.Row="2" Text="{Binding Summary}" Foreground="#b9cbd1" FontSize="15" TextWrapping="Wrap" Margin="0,8,0,0" />

        <Border Grid.Row="3" Background="#132b39" BorderBrush="#2b5367" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,16,0,14">
            <StackPanel>
                <TextBlock Text="Что произошло" Foreground="#7af0aa" FontWeight="Bold" />
                <TextBlock Text="{Binding Explanation}" Foreground="#d2e0e3" TextWrapping="Wrap" Margin="0,6,0,0" />
                <TextBlock Text="{Binding ModHint}" Foreground="#f2d59a" TextWrapping="Wrap" Margin="0,9,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Фрагменты лога" Foreground="#9eb8c3" FontWeight="Bold" />
            <TextBox Grid.Row="1"
                     Text="{Binding EvidenceText}"
                     IsReadOnly="True"
                     IsTabStop="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="#0c1925"
                     Foreground="#d2e0e3"
                     BorderBrush="#2b5367"
                     BorderThickness="1"
                     Padding="10"
                     FontFamily="Consolas"
                     FontSize="12"
                     Margin="0,7,0,8" />
            <TextBlock Grid.Row="2" Text="{Binding LogPath}" Foreground="#718b96" FontSize="11" TextTrimming="CharacterEllipsis" ToolTip="{Binding LogPath}" />
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Style="{StaticResource PrimaryButton}" Content="Открыть полный лог" Click="OpenLog_Click" />
            <Button Content="Закрыть" IsDefault="True" IsCancel="True" Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
