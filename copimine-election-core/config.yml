custom-block-visuals:
  enabled: true
  base-material: PAPER
  models:
    ATM:
      model-id: atm_terminal
      custom-model-data: 12002
      scale: 1.01
      offset-y: 0.5
      label: "Банк CopiMine"
    POLLING_STATION:
      model-id: polling_station_marker
      custom-model-data: 14002
      scale: 1.01
      offset-y: 0.5
      label: "Участок ЦИК"
    TAX_OFFICE:
      model-id: tax_office_marker
      custom-model-data: 14003
      scale: 1.01
      offset-y: 0.5
      label: "Налоговая"
    ARTIFACT_SHOP:
      model-id: artifact_shop_marker
      custom-model-data: 14004
      scale: 1.01
      offset-y: 0.5
      label: "Лавка"

# Заготовка президентского налога. Сейчас налоговый рантайм отключён в коде и на сайте:
# все активные платежные входы возвращают понятную ошибку, чтобы не списывать AR случайно.
# Если налог будет возвращён в релиз, включать его нужно отдельной миграцией и аудитом экономики.
# president-tax:
#   enabled: false
#   payment-interval-hours: 24
#   min-amount-ar: 0
#   max-amount-ar: 50
#   recipient: "active_president"
